# Next.js Configuration
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-here-generate-with-openssl-rand-base64-32

# MongoDB Database
# For local development: mongodb://localhost:27017/budget2025
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/?appName=budget-mdb
MONGODB_URI=mongodb://localhost:27017/budget2025

# Email Configuration for Loan Reminders
# You can use EITHER OAuth2 (recommended) OR App Password

# Your Gmail address (required for both methods)
SMTP_USER=your-email@gmail.com
SMTP_FROM=your-email@gmail.com

# === METHOD 1: OAuth2 (RECOMMENDED - More Secure) ===
# Get these from Google Cloud Console: https://console.cloud.google.com
# See GMAIL_OAUTH_SETUP.md for detailed setup instructions
GMAIL_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GMAIL_CLIENT_SECRET=your-google-client-secret
GMAIL_REFRESH_TOKEN=your-refresh-token-from-oauth-playground

# === METHOD 2: App Password (Legacy - Less Secure) ===
# Only use if OAuth2 is not configured
# Get App Password: https://myaccount.google.com/apppasswords
# The app will automatically fallback to this if OAuth2 is not configured
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_PASS=your-gmail-app-password

# Cron Job Security
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
CRON_SECRET_TOKEN=your-random-secret-token-here
